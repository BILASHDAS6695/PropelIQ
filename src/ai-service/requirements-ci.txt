# Lightweight requirements for CI — excludes heavy ML/OCR packages (spacy, pytesseract, pymupdf)
# that conflict with fastapi's dependency tree and are not needed for test execution.
# All service methods using those packages are raise NotImplementedError stubs.
fastapi==0.111.0
uvicorn[standard]==0.29.0
pydantic==2.7.1
pydantic-settings==2.2.1
python-multipart==0.0.9
python-dotenv==1.0.1
structlog==24.2.0
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
ruff==0.4.4
mypy==1.10.0
